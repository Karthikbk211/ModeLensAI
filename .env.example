# Postgres connection string (Neon, Supabase, etc.).
# Omit/leave blank to use a local SQLite file (modelens.db) instead.
DATABASE_URL=postgresql+psycopg2://user:password@host.neon.tech:5432/modelens?sslmode=require

# Enables the GenAI failure-explanation step on the New Analysis page.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
