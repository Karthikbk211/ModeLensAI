# Overfitting and Generalization
Overfitting occurs when a model captures patterns specific to training data that do not generalize to unseen data. Warning signs include a large gap between training and validation/test performance.
Possible investigation steps include cross-validation, comparing train and validation metrics, reducing model complexity, regularization, collecting more representative data, and checking for data leakage.
